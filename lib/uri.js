// Generated by CoffeeScript 1.7.1
(function() {
  var URI, dotPat, getParams, parentPat, urlPat;

  urlPat = /^(([^:/]+):\/\/([^/]*))?(\/(.*?))?(\?.*?)?(#.*)?$/;

  dotPat = /\/\.(?=\/|$)/g;

  parentPat = /^\/\.\.|\/[^/]+?\/\.\./g;

  URI = (function() {
    function URI(src, relative) {
      var match, _ref, _ref1;
      if (relative) {
        src = new URI(src).relativePath(relative);
      }
      if (match = src.match(urlPat)) {
        if (match[2]) {
          this.scheme = match[2].toLowerCase();
          this.host = match[3];
        }
        this.path = match[5] ? this.normalize(((this.scheme ? '/' : '') + match[5]).replace(dotPat, '')) : '/';
        this.search = (_ref = match[6]) != null ? _ref : '';
        this.fragment = (_ref1 = match[7]) != null ? _ref1 : '';
      }
    }

    URI.prototype.appendParams = function(char, old, str) {
      return "" + old + (old ? '&' : char) + str;
    };

    URI.prototype.appendSearch = function(str) {
      return this.search = this.appendParams('?', this.search, str);
    };

    URI.prototype.appendFragment = function(str) {
      return this.fragment = this.appendParams('#', this.fragment, str);
    };

    URI.prototype.normalize = function(path) {
      var replaced;
      while (true) {
        replaced = false;
        path = path.replace(parentPat, function(match) {
          replaced = true;
          return '';
        });
        if (!replaced) {
          break;
        }
      }
      return path;
    };

    URI.prototype.relative = function(path) {
      return new URI(this.relativePath(path));
    };

    URI.prototype.relativePath = function(path) {
      var u;
      u = new URI(path);
      if (u.scheme) {
        return u.toString();
      } else {
        return (this.scheme ? "" + this.scheme + "://" + this.host : '') + (path.match(/^\//) ? path : this.path.match(/\/$/) ? "" + this.path + path : "" + this.path + "/../" + path);
      }
    };

    URI.prototype.toString = function() {
      var _ref, _ref1;
      return (this.scheme ? "" + this.scheme + "://" + this.host : "") + this.path + ((_ref = this.search) != null ? _ref : "") + ((_ref1 = this.fragment) != null ? _ref1 : "");
    };

    URI.prototype.getSearchParams = function() {
      var _ref;
      return getParams((_ref = this.search) != null ? _ref : '');
    };

    URI.prototype.getFragParams = function() {
      var _ref;
      return getParams((_ref = this.fragment) != null ? _ref : '');
    };

    URI.prototype.paramString = function(paramObj) {
      var k, v;
      return ((function() {
        var _results;
        _results = [];
        for (k in paramObj) {
          v = paramObj[k];
          _results.push("" + k + "=" + v);
        }
        return _results;
      })()).join('&');
    };

    URI.prototype.setSearchParams = function(paramObj) {
      this.search = "?" + (this.paramString(paramObj));
      return this;
    };

    URI.prototype.setFragParams = function(paramObj) {
      this.fragment = "#" + (this.paramString(paramObj));
      return this;
    };

    URI.prototype.pathName = function() {
      if (!this.path) {
        console.log((new Error()).stack);
      }
      return (this.path.match(/\/[^/]*$/))[0].substring(1);
    };

    URI.prototype.pathParent = function() {
      if (!this.path) {
        console.log((new Error()).stack);
      }
      return (this.path.match(/^.*\/(?=[^/]*$)/))[0];
    };

    return URI;

  })();

  getParams = function(str) {
    var key, m, match, param, params, value, _i, _len, _ref;
    params = {};
    if (str !== '') {
      _ref = str.substring(1).split('&');
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        param = _ref[_i];
        match = param.match(/^([^=]+)=(.*)$/);
        if (match) {
          m = match[0], key = match[1], value = match[2];
          params[key] = decodeURIComponent(value);
        }
      }
    }
    return params;
  };

  if (typeof window !== "undefined" && window !== null) {
    window.URI = URI;
  } else {
    module.exports = URI;
  }

}).call(this);

//# sourceMappingURL=uri.map
