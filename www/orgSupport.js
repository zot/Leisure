// Generated by CoffeeScript 1.6.3
/*
Copyright (C) 2013, Bill Burdick, Tiny Concepts: https://github.com/zot/Leisure

(licensed with ZLIB license)

This software is provided 'as-is', without any express or implied
warranty. In no event will the authors be held liable for any damages
arising from the use of this software.

Permission is granted to anyone to use this software for any purpose,
including commercial applications, and to alter it and redistribute it
freely, subject to the following restrictions:

1. The origin of this software must not be misrepresented; you must not
claim that you wrote the original software. If you use this software
in a product, an acknowledgment in the product documentation would be
appreciated but is not required.

2. Altered source versions must be plainly marked as such, and must not be
misrepresented as being the original software.

3. This notice may not be removed or altered from any source distribution.
*/


(function() {
  var BS, DEL, ENTER, HL_TAGS, Headline, Keyword, Meat, Source, TAB, bindContent, checkExtraNewline, checkLast, cleanHeadline, defaultEnv, displaySource, editDiv, executeSource, fixupNodes, followingSpan, getLeft, getRight, getTags, getTextLine, getType, handleMutation, headlineRE, id, inCollapsible, isCollapsible, isDocNode, isEmptyCollapsible, isHeadlineNode, isMeatNode, isSourceNode, lazy, lz, markupGuts, markupNode, markupOrg, modifying, orgEnv, parseOrgMode, parseTags, removeTrailingNL, resolve, root, rz, setTags, show, sourceDiv, trailingNL, _ref, _ref1, _ref2;

  getType = require('./ast').getType;

  _ref = root = module.exports = require('./base'), resolve = _ref.resolve, lazy = _ref.lazy, defaultEnv = _ref.defaultEnv;

  rz = resolve;

  lz = lazy;

  _ref1 = require('./browserSupport'), TAB = _ref1.TAB, ENTER = _ref1.ENTER, BS = _ref1.BS, DEL = _ref1.DEL;

  _ref2 = require('./org'), parseOrgMode = _ref2.parseOrgMode, Headline = _ref2.Headline, Meat = _ref2.Meat, Keyword = _ref2.Keyword, Source = _ref2.Source, headlineRE = _ref2.headlineRE, HL_TAGS = _ref2.HL_TAGS, parseTags = _ref2.parseTags;

  editDiv = null;

  sourceDiv = null;

  modifying = false;

  markupOrg = function(text) {
    var org;
    if (text[text.length - 1] !== '\n') {
      text = text + '\n';
    }
    org = parseOrgMode(text);
    return markupNode(org, '');
  };

  markupNode = function(org, newline) {
    var content, pos;
    if (org instanceof Source) {
      pos = org.contentPos - org.offset - 1;
      content = org.text.substring(pos);
      return "" + (newline ? '<span>\n</span>' : '') + "<span data-org-type='" + org.type + "'><span data-org-type='text'>" + (org.text.substring(0, pos)) + "</span><span>" + content + "</span></span>";
    } else if (org instanceof Headline) {
      return "<span data-org-type='" + org.type + "'><span data-org-type='text'>" + (removeTrailingNL(org.text)) + "</span>" + (markupGuts(org)) + "</span>";
    } else {
      return "<span data-org-type='" + org.type + "'>" + newline + org.text + "</span>";
    }
  };

  isSourceNode = function(node) {
    return (node != null ? typeof node.getAttribute === "function" ? node.getAttribute('data-org-type') : void 0 : void 0) === 'source';
  };

  isHeadlineNode = function(node) {
    return (node != null ? typeof node.getAttribute === "function" ? node.getAttribute('data-org-type') : void 0 : void 0) === 'headline';
  };

  isMeatNode = function(node) {
    return (node != null ? typeof node.getAttribute === "function" ? node.getAttribute('data-org-type') : void 0 : void 0) === 'meat';
  };

  isDocNode = function(node) {
    return node != null ? typeof node.hasAttribute === "function" ? node.hasAttribute('maindoc') : void 0 : void 0;
  };

  markupGuts = function(org) {
    var c, newline, nl;
    if (!org.children.length) {
      return '<span>\n</span>';
    } else {
      newline = '\n';
      return ((function() {
        var _i, _len, _ref3, _results;
        _ref3 = org.children;
        _results = [];
        for (_i = 0, _len = _ref3.length; _i < _len; _i++) {
          c = _ref3[_i];
          nl = newline;
          newline = '';
          _results.push(markupNode(c, nl));
        }
        return _results;
      })()).join("");
    }
  };

  removeTrailingNL = function(str) {
    if (str[str.length - 1] === '\n') {
      return str.substring(0, str.length - 1);
    } else {
      return str;
    }
  };

  trailingNL = function(str) {
    if (str[str.length - 1] === '\n') {
      return '\n';
    } else {
      return '';
    }
  };

  fixupNodes = function(node) {
    var n, _i, _len, _ref3, _results;
    _ref3 = $(node).find('[data-org-type="headline"]');
    _results = [];
    for (_i = 0, _len = _ref3.length; _i < _len; _i++) {
      n = _ref3[_i];
      _results.push(setTags(n));
    }
    return _results;
  };

  bindContent = function(div, givenSourceDiv) {
    editDiv = div;
    sourceDiv = givenSourceDiv;
    fixupNodes(div);
    div.addEventListener('keydown', function(e) {
      var br, c, el, n, node, r, s;
      c = e.charCode || e.keyCode || e.which;
      if (c === TAB) {
        e.preventDefault();
        node = inCollapsible(getSelection().focusNode);
        if (node && !isEmptyCollapsible(node)) {
          modifying = true;
          $(node).toggleClass('collapsed');
          return modifying = false;
        }
      } else if (String.fromCharCode(c) === 'C' && e.altKey) {
        return executeSource(getSelection().focusNode);
      } else if (c === ENTER) {
        s = window.getSelection();
        r = s.getRangeAt(0);
        e.preventDefault();
        n = s.focusNode;
        if (n.nodeType === 3 && r.collapsed && r.startOffset === n.length && n.parentNode.getAttribute('data-org-type') === 'text') {
          br = document.createTextNode('\n');
          $(br).prependTo(followingSpan(n.parentNode));
          r.setStart(br, br.length);
          r.setEnd(br, br.length);
        } else {
          window.N = n;
          r.insertNode(br = document.createTextNode(checkExtraNewline(r, n, div)));
          br.parentNode.normalize();
        }
        r.collapse();
        s.removeAllRanges();
        return s.addRange(r);
      } else if (c === DEL || c === BS) {
        s = window.getSelection();
        r = s.getRangeAt(0);
        el = r.startContainer;
        if (r.collapsed && el.nodeType === 3 && el.parentElement.getAttribute('data-org-type') === 'text' && r.startOffset === el.length && el.parentElement.parentElement.classList.contains('collapsed')) {
          e.preventDefault();
          if (c === BS && el.length > 0) {
            el.nodeValue = el.nodeValue.substring(0, el.nodeValue.length - 1);
            r.setStart(el, el.length);
            r.collapse();
            s.removeAllRanges();
            return s.addRange(r);
          }
        }
      }
    });
    div.addEventListener('DOMCharacterDataModified', handleMutation, true);
    div.addEventListener('DOMSubtreeModified', handleMutation, true);
    return displaySource();
  };

  orgEnv = function(node) {
    return {
      write: function(str) {
        return console.log(str);
      }
    };
  };

  orgEnv.__proto__ = defaultEnv;

  id = lz(function(x) {
    return rz(x);
  });

  getLeft = function(x) {
    return x(id)(id);
  };

  getRight = function(x) {
    return x(id)(id);
  };

  show = function(obj) {
    if (typeof L_show !== "undefined" && L_show !== null) {
      return rz(L_show)(lz(obj));
    } else {
      return console.log(obj);
    }
  };

  executeSource = function(node) {
    var env, m, result, txt;
    if (isSourceNode(node)) {
      txt = $(node).text().substring($(node).find('[data-org-type="text"]').text().length);
      m = txt.match(/(^|\n)#\+end_src/i);
      if (m) {
        result = rz(L_baseLoadString)('notebook')(txt.substring(0, m.index));
        env = orgEnv(node);
        return runMonad(result, env, function(res) {
          res = res.head().tail();
          if (getType(res) === 'left') {
            return orgEnv.write("PARSE ERROR: " + (getLeft(res)));
          } else {
            return env.write(show(getRight(res)));
          }
        });
      } else {
        return console.log("No end for src block");
      }
    } else {
      return !isDocNode(node) && executeSource(node.parentElement);
    }
  };

  followingSpan = function(node) {
    var _ref3;
    return (_ref3 = node.nextElementSibling) != null ? _ref3 : $('<span></span>').appendTo(node.parentNode)[0];
  };

  checkExtraNewline = function(range, n, parent) {
    if (range.collapsed && n.nodeType === 3 && range.startOffset === n.length && n.textContent[n.length - 1] !== '\n') {
      return checkLast(n, parent);
    } else {
      return '\n';
    }
  };

  checkLast = function(n, parent) {
    if (n === parent) {
      return '\n\n';
    } else if (n.nextSibling) {
      return '\n';
    } else {
      return checkLast(n.parentNode, parent);
    }
  };

  getTags = function(headline) {
    if (headline.getAttribute('dirty')) {
      cleanHeadline(headline);
      setTags(headline);
    }
    return headline.getAttribute('data-org-tags');
  };

  setTags = function(headline) {
    var m, tags;
    m = headline.firstChild.textContent.match(headlineRE);
    tags = ((m && parseTags(m[HL_TAGS])) || []).join(' ');
    if (headline.getAttribute('data-org-tags') !== tags) {
      return headline.setAttribute('data-org-tags', tags);
    }
  };

  cleanHeadline = function(node) {
    modifying = true;
    node.removeAttribute('dirty');
    return modifying = false;
  };

  handleMutation = function(evt) {
    var node;
    if (!modifying) {
      modifying = true;
      if ((node = inCollapsible(evt.srcElement)) && (node.getAttribute('data-org-type') === 'headline')) {
        node.setAttribute('dirty', 'true');
      }
      displaySource();
      return modifying = false;
    }
  };

  displaySource = function() {
    return $(sourceDiv).html('').text($(editDiv).text());
  };

  isCollapsible = function(node) {
    var _ref3;
    return (_ref3 = node.getAttribute('data-org-type')) === 'headline' || _ref3 === 'source';
  };

  inCollapsible = function(node) {
    if (node.nodeType === 1) {
      if (isCollapsible(node)) {
        return node;
      } else {
        return (node.getAttribute('data-org-type') === 'text') && inCollapsible(node.parentElement);
      }
    } else {
      return node.nodeType === 3 && inCollapsible(node.parentElement);
    }
  };

  isEmptyCollapsible = function(node) {
    var firstLine;
    firstLine = getTextLine(node);
    return isCollapsible(node) && firstLine.textContent + '\n' === node.textContent;
  };

  getTextLine = function(node) {
    var c;
    c = node.firstElementChild;
    while (c) {
      if (c.getAttribute('data-org-type') === 'text') {
        return c;
      }
      c = c.nextElementSibling;
    }
    return null;
  };

  root.markupOrg = markupOrg;

  root.bindContent = bindContent;

  root.cleanHeadline = cleanHeadline;

  root.getTags = getTags;

}).call(this);

/*
//@ sourceMappingURL=orgSupport.map
*/
